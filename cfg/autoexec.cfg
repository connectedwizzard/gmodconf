// Launch
// -nojoy -nosteamcontroller -softparticlesdefaultoff -nohltv -particles 1 -useforcedmparms -noforcemaccel -noforcemspd -primarysound -snoforceformat +exec autoexec.cfg

// Exec
exec overrides/gfx
exec overrides/filter
// exec overrides/afk
// exec overrides/zz

// Alias
alias "q" "quit"
alias "d" "disconnect"
alias "c" "connect"
alias "k" "kill"
alias "e" "exec"

// Bind
bind "k" kill

// Connection
// rate 30000
// cl_cmdrate 66
// cl_updaterate 66

// gmod_mcore_test 1

// Echo
echo "loaded config"
echo "loaded config"
echo "loaded config"
echo "loaded config"
echo "loaded config"
echo "loaded config"
